name: "notify build start"

on:
  workflow_call:
    inputs:
      WORKFLOW_NAME:
        required: false
        type: string
    secrets:
      SLACK_BUILD_UPDATE:
        required: true

jobs:
  start:
    runs-on: ubuntu-latest
    steps:
      - id: slack
        name: Notify build start
        uses: slackapi/slack-github-action@v1.23.0
        with:
          channel-id: ${{ secrets.SLACK_CHANNEL_ID }}
          payload: |
            {
              "attachments": [
                {
                  "color": "dbab09",
                  "fields": [
                    {
                      "title": "Build started for ${{ github.repository }}",
                      "short": false,
                      "value": "Status: In Progress"
                    },
                    {
                      "value": "${{ github.server_url }}/${{ github.repository }}/actions/workflows/${{ github.run_id }}|link>"
                    }
                  ]
                }
              ]
            }

        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BUILD_UPDATE}}
