name: Hello world

on:
  push:
   branches: [ $default-branch ]
  pull_request:
   branches: [ $default-branch ]

jobs:
  hello:
   runs-on: ubuntu-latest
   steps:
    - uses: actions/checkout@v3

    - name: Say hello
      run: echo "Hello World"